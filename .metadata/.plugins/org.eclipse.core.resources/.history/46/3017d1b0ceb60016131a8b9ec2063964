precision mediump float;

uniform sampler2D mainTex;
uniform float objectOpacity;
uniform vec3 objectColor;
varying vec2 UVs;

void main()
{
	UVs.y = 1.0 - UVs.y;
	gl_FragColor = vec4(objectColor, texture(mainTex, UVs).r * objectOpacity);
}
