precision mediump float;

uniform sampler2D mainTex;
uniform float objectOpacity;
uniform vec3 objectColor;
varying vec2 UVs;

void main()
{
	vec2 v = vec2(UVs.x, 1.0 - UVs.y);
	gl_FragColor = vec4(objectColor, texture2D(mainTex, v).r * objectOpacity);
}
