#ifndef SHADERS_H
#define SHADERS_H

#include <GL/glew.h>
#include "errorHandling.h"

void shaders_init(float left, float top, float width, float height);
void shaders_setSceneLayout(float left, float top, float width, float height);
GLuint shaders_getAttributeIndex_vertexPosition();
GLuint shaders_getAttributeIndex_vertexTextureCoordinate();

//void shaders_useColor(float xPos, float yPos, float xScale, float yScale, float red, float green, float blue, float rotation = 0);
//void shaders_useTexture(float xPos, float yPos, float xScale, float yScale, int textureId, float rotation = 0);
//void shaders_useBlur(float xPos, float yPos, float xScale, float yScale, int textureId, float blurAmount, float rotation = 0);

#endif // SHADERS_H
